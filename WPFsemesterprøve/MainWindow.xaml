<Window x:Name="listSessions" x:Class="WPFsemesterprøve.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFsemesterprøve"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>

        <ListBox x:Name="SessionsDescriptionListBox" 
                 Grid.Column="1" 
                 Grid.Row="0" 
                 SelectionChanged="ListBox_SelectionChanged"
                 DisplayMemberPath="Description" 
                 FontSize="18"
        />
        <ListBox x:Name="SelectedSessionListBox"
                 Grid.Column="4" 
                 Grid.Row="0" 
                 DisplayMemberPath="Name" 
                 FontSize="36"
        />
        <Button x:Name="JoinSessionButton" 
                Grid.Column="4" 
                Grid.Row="1" 
                Content="Join Session" 
                Click="JoinSessionButton_Click" 
                FontSize="36"
                
        />
        <TextBlock x:Name="LoggedInAsTextBlock" 
                   Text=""
                   Grid.Column="1" 
                   Grid.Row="1" 
                   FontSize="18" 
        />
    </Grid>

</Window>
